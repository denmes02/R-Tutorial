---
title: "R Tutorial"
output: learnr::tutorial
runtime: shiny_prerendered
---

```{r setup, include=FALSE}
library(learnr)
library(readxl)

file_path <- "Player_Per_Game_Enzo_Famboupe_Abbas_Datensatz.xlsx"
data <- read_excel(file_path, sheet = "Player Per Game")
```


## Datenbasis
### Beschreibung der Datenherkunft
This code computes the answer to one plus one, change it so it computes two plus two:

### Übung zur Datendarstellung

```{r addition, exercise=TRUE}
1 + 1
```

## Datenaufbereitung
Bei diesem interaktiven Tutorial lernen Sie, wie das Einfügen und korrigieren von Inkosistenzen, sowie das Entfernen von Leerstellen funktioniert.

### Einfügen

question("Welche Funktion wird genutzt, um XLS-Dateien in R einzulesen?",
  answer("read_excel", correct = TRUE),
  answer("load_excel", message = "Diese Funktion existiert nicht in R."),
  answer("import_excel", message = "Diese Funktion existiert nicht in R.")
  allow_retry = TRUE
)

### Inkonsistenzen und Leerstellen beheben


## Datenanalyse
In diesem Tutorial lernen Sie, wie Eigenschaften von Daten aufgerufen werden und wie diese Dargestellt werden.

### Mittelwert
Berechnen Sie den Mittelwert mit `mean()` von `data`

```{r mean, exercise=TRUE}
mean()
```

### Median
### Varianz
### Standardabweichung
### Lineare Regression
### Hypothesentest

## Ergebnispräsentation


## Teaminfos


